import type { CellConfig } from "../lib/types.js";

export const cellControl: CellConfig = {
  name: "cell-control",
  description: "Microsoft AL extension only — control baseline.",
  extensionIds: ["ms-dynamics-smb.al"],
};
